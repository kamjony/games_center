import 'package:flutter/material.dart';

class GameTrailersItem extends StatelessWidget {
  const GameTrailersItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
